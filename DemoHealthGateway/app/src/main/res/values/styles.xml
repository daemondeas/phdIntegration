<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarStyle">@style/HealthGateway.ActionbarStyle</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="HealthGateway.ActionbarStyle" parent="AppTheme">
        <item name="android:titleTextStyle">
            @style/HealthGateway.ActionbarStyle.TitleTextStyle
        </item>
    </style>

    <style name="HealthGateway.ActionbarStyle.TitleTextStyle">
        <item name="android:textColor">@color/black</item>
    </style>

</resources>
